#+TITLE: Thinking in Java chapter5 笔记和习题 
#+AUTHOR:    emacsun
#+EMAIL:     emacsun@163.com
#+DATE:      
#+DESCRIPTION:  
#+KEYWORDS:  
#+CATEGORIES: 
#+SETUPFILE: ~/.emacs.d/org-templates/java-level2.org
#+HTML_HEAD_EXTRA: <a id="pdf"  href="./TIJ-ch05.pdf"><img src="../../img/assets/pdf.png"></a>
#+LaTeX_CLASS: cn-article
 
* 简介
*Controlling Execution* 这一章涉及的大多知识点和 =C/C++= 中已经学过的类似，仅仅有一小部分不同，比如去掉了饱受争议的 =goto= 语句。本文记录控制语句学习过程中需要记录的知识点。

* 只有 =boolean= 有真假
  [2017-02-27 Mon 20:08]

  由于Java是强类型语言， 只有 =boolean= 有真假。尤其要注意的是不能把 =int= 或者 =float= 的非零值当 =true= 来处理。 在使用 =if= 语句时， 比如 =if(a)= ，条件判断语句 =a= 必须是 =boolean= 类型。如果要用非 =boolean= 类型来做判断，则需要用这个非 =boolean= 类型的变量来生成一个 =boolean= 变量。比如对非布尔变量 =a= 做对比 =(a == 0)= .
* 注意 =a=b= 问题
  [2017-02-27 Mon 20:34]

在 =if= 的条件判断句中，最容易犯的错误是: =a=b= . 本意是要比较 =a= 和 =b= 是否相等，但是 =a=b= 是赋值语句。这样的语句在 =C/C++= 中不会报错，但是存在逻辑错误，但是在java中，这样的语句一定会报错。看代码：
#+begin_src java -n
//:control/IfElse.java
import static net.mindview.util.Print.*;
public class IfElse
{
    static int result = 0;
    static void test(int testval, int target)
    {
        if (testval > target) {
            result = +1;
        }else if (testval < target ) {
            result = -1;
        }
        if (testval = target) {
            result = 0;
        }
    }
    public static void main(String args[])
    {
        test(6,5);
        print(result);
        test(5,6);
        print(result);
        test(1,1);
        print(result);
    }
}
#+end_src
第14行代码在编译的时候会报错：
#+begin_example
IfElse.java:14: error: incompatible types: int cannot be converted to boolean
        if (testval = target) {
#+end_example
=Java= 在编译阶段就避免了代码中可能因为 =a=b=  导致的逻辑错误。我认为这是 =Java= 相对于 =C/C++= 进步的地方。
* Random的导入
  [2017-03-01 Wed 20:07] 

在完成 =Exercise02= 的过程中，忘记导入 =import java.util.*= 导致无法使用 =Random= 类， 特此记录，看代码：
#+begin_src java -n
import java.util.*;
public class Exercise0502
{
    public static void main(String args[])
    {
        Random rand = new Random();

        int target = rand.nextInt();
        int source;
        for (int j=0; j < 25; j++) {
            source = rand.nextInt();
            if (source > target)
                System.out.println( "target " + target + 
                       " < " + " source " + source);
            else if (source < target) 
                System.out.println( "target " + target + 
                       " > " + " source " + source);
            else
                System.out.println( "target " + target + 
                       " = " + " source " + source);
        }

    }
}
#+end_src
* =foreach=
  [2017-03-01 Wed 21:01]

=foreach= 是 =C= 语言中不存在的一种循环表达方式，看代码：
#+begin_src java -n
import java.util.*;
public class ForEachFloat
{
    public static void main(String[] args)
    {
        Random rand = new Random();
        float f[] =  new float[10];
        for (int i = 0; i < 10; i++) {
            f[i] = rand.nextFloat();
        }
        for (float x : f) {
            System.out.println(x);
        }

    }
}
#+end_src
=foreach= 可以配合任何 =Iterable= 对象。 =Python= 中有一类数据叫做 =iterator= 其抽象程度比 =Java= 的 =foreach= 中用到的 =Iterable= 对象更高。


